import React from 'react';

// can be made stateless functional component so, do it
// we can get rid of `props` also
const Title = () => (
	<div>
		<h1>Weather Finder</h1>
		<h3>Find temperature, humidity, and many more.</h3>
	</div>
);

export default Title